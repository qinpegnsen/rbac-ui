import { LimitChildDirective } from './limit-child.directive';

describe('LimitChildDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitChildDirective();
    expect(directive).toBeTruthy();
  });
});
