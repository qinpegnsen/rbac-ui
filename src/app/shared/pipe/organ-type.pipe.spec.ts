import { OrganTypePipe } from './organ-type.pipe';

describe('OrganTypePipe', () => {
  it('create an instance', () => {
    const pipe = new OrganTypePipe();
    expect(pipe).toBeTruthy();
  });
});
